# amc-ticket-tracker

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues (`Pratyush8991/amc-ticket-tracker`) via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Traditional public-facing labels: `needs-triage`, `needs-info`, `ready`, `planned`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
