__pycache__/
*.pyc
.venv/
.claude/

# Runtime dedup cache the watcher writes as it runs — local only, not part of the repo.
state.json

# config.json is committed as a working example (the Actions runner reads it, and it
# holds no secrets — just public showtime IDs). Your ntfy topic comes from the
# NTFY_TOPIC secret/env, so it never needs to live in the file.
